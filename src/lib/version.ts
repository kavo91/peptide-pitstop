/**
 * Single source of truth for the user-facing app version shown in the footer
 * version tracker ("… · v1.0"). Bump this string on each release; keep it in
 * sync with package.json's "version" (display form may omit the patch, e.g.
 * package 1.0.0 → display "1.0").
 */
export const APP_VERSION = "1.0";
